[package]
name = "grasp"
version = "0.1.0"
edition = "2024"

[dependencies]
pretty_assertions = "1.4.1"
linkme = {version = "0.3", optional = true}
macros = {path = "../macros"}

[features]
meta = ["macros/meta", "dep:linkme"]
